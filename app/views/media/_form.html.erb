<%= form_with(model: medium) do |form| %>
  <% if medium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medium.errors.count, "error") %> prohibited this medium from being saved:</h2>

      <ul>
        <% medium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :rss_url, style: "display: block" %>
    <%= form.text_field :rss_url %>
  </div>

  <div>
    <%= form.label :icon_path, style: "display: block" %>
    <%= form.text_field :icon_path %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
